import Vue from 'vue'
import {
    Button,
    Input,
    Form,
    Message,
    FormItem,
} from 'element-ui'

Vue.use(Button)
Vue.use(Input)
Vue.use(Form)
Vue.use(FormItem)

Vue.prototype.$message = Message;